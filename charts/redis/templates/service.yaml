version: v1
kind: Service
metadata:
  name: {{ .Values.appname }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.appname }}
  ports:
  - protocol: TCP
    port: {{ .Values.servicePort }}
    targetPort: {{ .Values.containerPort }}